apiVersion: v1
kind: Service
metadata:
  name: marketing-svc
  namespace: business
  labels:
    app: marketing
spec:
  selector:
    app: marketing
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
